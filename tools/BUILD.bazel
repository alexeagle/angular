# Executes the workspace_status_command and provides the result.
# See the section on stamping in BAZEL.md
genrule(
    name = "stamp_data",
    outs = ["stamp_data.txt"],
    stamp = True,
    cmd = "cat bazel-out/volatile-status.txt > $@",
    visibility = ["//:__subpackages__"],
)